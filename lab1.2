// Даны два списка. Определите, совпадают ли множества их элементов

#include <iostream>
#include <list>
using namespace std;

int main()
{
	list<int> list1;
	list<int> list2;

	for (int i = 0; i < 5; ++i)
		list1.push_back(i);
	for (int i = 0; i < 5; ++i)
		list2.push_back(i);

	if (list2 == list1)
		cout << "Equal";
	else cout << "Do not equal";
	cout << endl << endl;

	system("pause");
}
